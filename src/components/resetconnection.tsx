import React from 'react';
import "../styles/resetconnection.css";

const Resetconnection = () => {
    return (
        <div className={"resConnEl"}>
            <button className={"button"}>Reset Connection</button>
        </div>
    );
};

export default Resetconnection;